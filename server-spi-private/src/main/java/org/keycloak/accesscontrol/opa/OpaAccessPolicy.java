package org.keycloak.accesscontrol.opa;

import org.keycloak.accesscontrol.AccessPolicy;

public class OpaAccessPolicy extends AccessPolicy {

    public OpaAccessPolicy(String id, String name) {
        super(id, name);
    }
}
